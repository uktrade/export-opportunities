<!DOCTYPE html>
<html lang="en-GB">
<head>
  <title><%= t('organisation.name') %> - International Business Opportunities</title>
  <%= csrf_meta_tags %>
  <%= render partial: 'international/includes/styles' %>
  <%= render partial: 'international/includes/scripts' %>
</head>
<body>
  <%= render partial: 'international/includes/header' %>

  <main id="main" role="main">
    <%= content_for?(:errors) ? yield(:errors) : "" %>
    <%= content_for?(:content) ? yield(:content) : yield %>
  </main>

  <%= render partial: 'international/includes/footer' %>
</body>
</html>
