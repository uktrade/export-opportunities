.summary-list
  %ul.navigation
    - list.each do |item|
      - if item[:images].present?
        %li
          %picture
            %source{:srcset=>"#{image_url(item[:images][:small])}", :media=>"(max-width: 768px)"}
            %img{:src=>"#{image_url(item[:images][:large])}", :alt=>item[:alt]}
          = link_to  item[:title], item[:url]
