%p.comment
  %span.text
    = opportunity_comment.message
  %span.metadata
    %span.name= opportunity_comment.posted_by_author? ? "#{opportunity_comment.author_name} (Opportunity author)" : opportunity_comment.author_name
    %span.created
      on
      =opportunity_comment.created_at.to_s(:admin_history)
