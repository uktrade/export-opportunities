<% if @count > 1 %>
<%= @count %> matching opportunities

You have <%= @count %> opportunities that match your criteria
<% else %>
<%= @count %> matching opportunity

You have <%= @count %> opportunity that matches your criteria
<% end %>


<% @opportunities.each do |opp| %>
<% opportunity = OpportunityPresenter.new(self, opp) %>

<%= opportunity.title_with_country %>

<%= opportunity.teaser %>

Opportunity expires: <%= opportunity.expires %>
<%= opportunity_url(opp) %>

-------------------------

<% end %>


View all matching opportunities at: 
<%= "#{Figaro.env.DOMAIN}/email_notifications/#{@encrypted_user_id}" %>

You can also search for other export opportunities at: 
<%= root_url %>


You are receiving this email because you signed up for Export Opportunities email alerts.

Manage your alerts
<%= Figaro.env.SUD_PROFILE_PAGE_EMAIL_ALERTS.to_s %>

Unsubscribe from all alerts
<%= "#{Figaro.env.DOMAIN}/email_notifications/unsubscribe_all/#{@encrypted_user_id}" %>

View privacy statement
<%= Figaro.env.PRIVACY_AND_COOKIES_URL.to_s %>

Give feedback at
<%= Figaro.env.CONTACT_US_FORM.to_s %>

