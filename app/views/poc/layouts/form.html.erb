<!DOCTYPE html>
<html lang="en-gb">
<head>
  <title><%= t('site_name') %></title>
  <%= csrf_meta_tags %>
  <%= render partial: 'includes/poc_head_tags' %>
  <%= render partial: 'includes/poc_styles' %>
  <%= render partial: 'includes/poc_scripts' %>

  <%= stylesheet_link_tag 'transformation/pages/form' %>
  <%= javascript_include_tag 'transformation/dit.page.form' %>
  <%= yield :action_specific_head %>
</head>
<body>
  <%= render partial: 'includes/header_international' %>

  <main id="main" role="main">
    <%= content_for?(:errors) ? yield(:errors) : "" %>
    <%= content_for?(:content) ? yield(:content) : yield %>
  </main>

  <%= render partial: 'includes/footer_international' %>
</body>
</html>
